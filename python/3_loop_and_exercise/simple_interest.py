#  P*R*T / 100

amount = float(input("Enter amount: "))

interest = float(input("Enter interest per month: "))

time = float(input("Enter time: "))

si = (amount * interest * time) / 100

print("Simple interest of given amount is: ", si)
